# ANOTHER DOT 랜딩 페이지 (Next.js)

로컬에서 확인하려면 다음 명령을 실행하세요.

```bash
cd C:\AI\homepage-anotherdot
set npm_config_cache=C:\AI\.npm-cache
npm install
npm run dev
```

브라우저에서 아래 주소를 열면 제작한 페이지를 확인할 수 있습니다.

- http://localhost:3000

빌드 검사(타입/번들 검증)는 아래 명령입니다.

```bash
npm run build
```
